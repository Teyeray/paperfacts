"""Builders and helpers for the test suite.

These live outside ``conftest.py`` because they are ordinary functions and classes that any test can
import explicitly. ``conftest.py`` is reserved for pytest hooks and fixtures, which arrive implicitly.
"""
